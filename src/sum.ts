/**
 * Sums the two supplied numbers together
 * @param a first number to sum
 * @param b second number to sum
 * @returns number
 */
export function sum(a: number, b: number): number {
  return a + b;
}